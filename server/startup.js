Meteor.startup(function(){
	
	// if(Incidents.find({}).count() == 0) {
	// 	Incidents.insert({userName:"bob", createdAt: Date(), message: "Hello world", likes: 0});
	// 	Incidents.insert({userName:"time", createdAt: Date(), message: "Heleeeelo world", likes: 3});
	// }
});
